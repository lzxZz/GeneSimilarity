import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.nio.file.StandardOpenOption;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.HashSet;

class TermSim
{
    ArrayList<String> termPair; //待计算的术语对


    private static void init_data()
    {
        //读取带计算的术语对

        //初始化本体结构，id-son id-children id-ancestor

        // 初始化注释数据，id-genes gene-ids
    }

    //计算术语对的相似度，参数为网络数据文件，输出文件，线程数
    public static void Calculator(String net_file, String out_file, int thread_count)
    {
        
        init_data();
    }
}